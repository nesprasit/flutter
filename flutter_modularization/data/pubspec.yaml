name: data
description: A new Flutter package project.
version: 0.0.1
homepage:
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

   # This is our modular
  dependencies:
    path: "../dependencies"
  commons:
    path: "../commons" 
  domain:
    path: "../domain"
  
  # Implementing lib
  dio:
  alice:
  logger:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
