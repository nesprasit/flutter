name: dependencies
description: Dependencies Module
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^4.0.0
  provider: ^6.0.0
  get_it: ^7.1.4
  alice: ^0.2.4
  json_annotation: ^4.0.1
  retrofit: ^2.0.1
  logger: ^1.0.0
  shared_preferences: ^2.0.7
  
  #UI
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.1
  json_serializable: ^4.1.4

flutter:
